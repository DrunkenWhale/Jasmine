syntax = "proto3";

package jasminepb;

option go_package = "/";

message Request{
  string node = 1;
  string key = 2;
}

message Response{
  bytes values = 1;
}

service NodeCache{
  rpc Get(Request) returns (Response);
}